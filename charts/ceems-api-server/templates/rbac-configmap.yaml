{{- if .Values.kubeRBACProxy.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "ceems-api-server.fullname" . }}-rbac-config
  namespace: {{ include "ceems-api-server.namespace" . }}
data:
  config-file.yaml: |+
    authorization:
      resourceAttributes:
        namespace: {{ template "ceems-api-server.namespace" . }}
        apiVersion: v1
        resource: services
        subresource: {{ template "ceems-api-server.fullname" . }}
        name: {{ template "ceems-api-server.fullname" . }}
{{- end }}
