{{- if (not (empty .Values.webConfig)) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "ceems-lb.fullname" . }}-web-config
  namespace: {{ include "ceems-lb.namespace" . }}
stringData:
  config.yaml: |-
  {{- with .Values.webConfig }}
  {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
