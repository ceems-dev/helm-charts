{{- if and .Values.redfishProxy.enabled .Values.redfishProxy.kubeRBACProxy.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "ceems-exporter.redfish-proxy.fullname" . }}-rbac-config
  namespace: {{ include "ceems-exporter.redfish-proxy.namespace" . }}
data:
  config-file.yaml: |+
    authorization:
      resourceAttributes:
        namespace: {{ template "ceems-exporter.redfish-proxy.namespace" . }}
        apiVersion: v1
        resource: services
        subresource: {{ template "ceems-exporter.redfish-proxy.fullname" . }}
        name: {{ template "ceems-exporter.redfish-proxy.fullname" . }}
{{- end }}
